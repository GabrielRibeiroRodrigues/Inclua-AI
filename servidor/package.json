{
  "name": "inclua-ai-servidor",
  "version": "1.0.0",
  "description": "Servidor de API do Inclua-AI com Google Gemini",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "lint": "echo 'Linting JavaScript files...' && node --check server.js",
    "health": "curl -f http://localhost:3000/health || exit 1",
    "clean": "node -e \"console.log('Limpando logs...'); process.exit(0)\""
  },
  "keywords": ["accessibility", "ai", "gemini", "api", "inclusao"],
  "author": "Gabriel Ribeiro Rodrigues",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0"
  }
}
